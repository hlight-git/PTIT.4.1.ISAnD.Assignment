package model;

public class WarehouseWorker extends Employee{
    public WarehouseWorker(int id, String username, String password, String name, String phone, String position) {
        setId(id);
        setUsername(username);
        setPassword(password);
        setName(name);
        setPhone(phone);
        setPosition(position);
    }
}
